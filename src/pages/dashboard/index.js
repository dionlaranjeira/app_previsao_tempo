import React from 'react'
import {Text, SafeAreaView} from 'react-native'

export default function Dashboard(){
    return(
        <SafeAreaView>
            <Text>Dashboard</Text>
        </SafeAreaView>
    );
}